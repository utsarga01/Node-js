// API =Application Programming Interface

// ? Portal
// Web App
// ? UI => Vanilla,React,Vue,Angular,Svelte
// ? Mobile App
// 1.Hybrid
// ? Flutter
// ? React Native

// 2. Native
// ? Android (Kotlin/Java)
// ? swift(ios)

// ? Server is same.
// ? Database => permanently store data
// ? Server side language
// ? java => Spring Boot
// ? js => Node => Express / Nest js
// ? Python => Django / Flask
// ? PHP => Laravel

 // API
 // ?REST => Representational Stateless
 // ?SOAP
 // ?GraphQL => client has power to decide which data to fetch

// API => acts as communicating bridge between client and server
// client side = frontend = ui(user interface) =customer side
// server side = business logic = backend

 // API => starts from request and end s with response

 // ? request has methods
 // ? POST => add/create
 // ? GET => read/retrieve
 // ? PUT => edit/update
 // ? DELETE => delete/remove


 // ? response has status code
 // ? response can be either success or error
 // ? success => 200-299
 // ?error
 // ?error can be client side or server side
 // ?client side => 400 to 499
 // ? server side => 500 to 599